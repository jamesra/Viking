@model Viking.Identity.Server.WebManagement.Models.UserViewModels.BulkPermissionsEditViewModel

@{
    ViewData["Title"] = "Bulk Edit Permissions";
    ViewData["Breadcrumbs"] = new List<(string Text, string Url)>
    {
        ("Dashboard", Url.Action("Index", "Home")),
        ("Volumes", Url.Action("Index", "Volumes")),
        ("Bulk Edit Permissions", null)
    };
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2><i class="bi bi-shield-check"></i> Bulk Edit Permissions</h2>
    <a asp-action="Index" asp-controller="Volumes" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Back to Volumes
    </a>
</div>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle"></i> @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="alert alert-info">
    <i class="bi bi-info-circle"></i> <strong>Bulk Operation:</strong> You are editing permissions for <strong>@Model.Volumes.Count</strong> volume(s). Changes will be applied to all selected volumes.
</div>

<div class="card card-modern mb-4">
    <div class="card-header bg-warning text-dark">
        <h5 class="mb-0"><i class="bi bi-hdd-stack"></i> Selected Volumes</h5>
    </div>
    <div class="card-body">
        <div class="row">
            @foreach (var volume in Model.Volumes)
            {
                <div class="col-md-6 col-lg-4 mb-2">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-hdd entity-icon-volume me-2"></i>
                        <div>
                            <strong>@volume.Name</strong>
                            <br><small class="text-muted">@volume.OrganizationName</small>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<form asp-action="BulkEdit" id="bulkPermissionsForm">
    @for (int i = 0; i < Model.SelectedVolumeIds.Count; i++)
    {
        <input type="hidden" asp-for="SelectedVolumeIds[i]" />
    }
    
    <partial name="~/Views/Shared/_ResourcePermissionsGridViewPartialView.cshtml" 
             model="@(new Viking.Identity.Server.WebManagement.Models.UserViewModels.ResourcePermissionsEditGridViewModel 
             { 
                 AvailablePermissions = Model.AvailablePermissions, 
                 UserPermissions = Model.UserPermissions, 
                 GroupPermissions = Model.GroupPermissions 
             })" />
    
    <div class="card card-modern mt-4">
        <div class="card-body">
            <div class="alert alert-warning">
                <i class="bi bi-exclamation-triangle"></i> <strong>Warning:</strong> This will apply the selected permissions to all @Model.Volumes.Count volume(s). Make sure this is what you want before proceeding.
            </div>
            <div class="form-group mt-4">
                <button type="submit" class="btn btn-warning btn-lg">
                    <i class="bi bi-save"></i> Apply to All @Model.Volumes.Count Volume(s)
                </button>
                <a asp-action="Index" asp-controller="Volumes" class="btn btn-outline-secondary btn-lg ms-2">
                    <i class="bi bi-x-circle"></i> Cancel
                </a>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#bulkPermissionsForm').on('submit', function() {
                if (!confirm('Are you sure you want to apply these permissions to all @Model.Volumes.Count selected volume(s)? This action cannot be undone.')) {
                    return false;
                }
                var $submitBtn = $(this).find('button[type="submit"]');
                $submitBtn.prop('disabled', true).html('<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Applying...');
            });
            
            // Add visual feedback for checkbox changes
            $('.permission-checkbox').change(function() {
                var checkbox = $(this);
                if (checkbox.is(':checked')) {
                    checkbox.closest('td').addClass('table-success');
                } else {
                    checkbox.closest('td').removeClass('table-success');
                }
            });
        });
    </script>
}


