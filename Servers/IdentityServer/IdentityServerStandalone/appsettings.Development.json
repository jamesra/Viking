{
  "VikingIdentityServerOptions": {
    "Authority": "https://identity.codepharm.net:${IDENTITY_STANDALONE_CONTAINER_HTTPS_PORT}/",
    "ManagementPublicUrl": "https://identity.codepharm.net:${IDENTITY_MANAGEMENT_HTTPS_PORT}/",
    "Secret": "${IDENTITY_SERVER_SECRET}",
    "ApiSecret": "${IDENTITY_SERVER_API_SECRET}",
    "MvcSecret": "${IDENTITY_SERVER_MVC_SECRET}",
    "RoVikingSecret": "${IDENTITY_SERVER_RO_VIKING_SECRET}",
    "SbfsemToolsClientSecret": "${SBFSEM_TOOLS_CLIENT_SECRET}",
    "ApiScopes": [
      {
        "Name": "Viking.Annotation",
        "Description": "Access to Annotate a volume"
      }
    ]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.AspNetCore.HttpsPolicy": "Information"
    }
  }
}