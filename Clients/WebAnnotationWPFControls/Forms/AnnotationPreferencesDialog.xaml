<Window x:Class="WebAnnotation.WPF.Forms.AnnotationPreferencesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:WebAnnotation.WPF.Forms"
        Title="Annotation Preferences" 
        Height="600" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5,5,5,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="xctk:IntegerUpDown">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="25"/>
        </Style>
        <Style TargetType="xctk:DoubleUpDown">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="25"/>
        </Style>
        <Style TargetType="Expander">
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Basic Settings -->
                <Expander Header="Basic Settings" IsExpanded="True" Margin="10">
                    <StackPanel Margin="10">
                        <!-- Num Sections In Memory -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Label Grid.Row="0" Grid.Column="0" Content="Sections in Memory:"/>
                            <xctk:IntegerUpDown Grid.Row="0" Grid.Column="1" 
                                              Value="{Binding NumSectionsInMemory}" 
                                              Minimum="1" 
                                              Maximum="100"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                     Margin="10,0,10,10"
                                     FontSize="10"
                                     Foreground="Gray"
                                     TextWrapping="Wrap"
                                     Text="Number of sections to keep in memory. Minimum of 3 recommended for smooth navigation."/>
                        </Grid>

                        <!-- Num Sections Loading -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Concurrent Section Loads:"/>
                            <xctk:IntegerUpDown Grid.Column="1" 
                                              Value="{Binding NumSectionsLoading}" 
                                              Minimum="1" 
                                              Maximum="50"/>
                        </Grid>

                        <!-- Location Text Scale Factor -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Location Text Scale:"/>
                            <xctk:DoubleUpDown Grid.Column="1" 
                                             Value="{Binding LocationTextScaleFactor}" 
                                             Minimum="0.1" 
                                             Maximum="50.0"
                                             Increment="0.5"
                                             FormatString="F1"/>
                        </Grid>

                        <!-- Reference Location Text Scale Factor -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Reference Text Scale:"/>
                            <xctk:DoubleUpDown Grid.Column="1" 
                                             Value="{Binding ReferenceLocationTextScaleFactor}" 
                                             Minimum="0.1" 
                                             Maximum="50.0"
                                             Increment="0.5"
                                             FormatString="F1"/>
                        </Grid>

                        <!-- Default Closed Line Width -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Default Line Width:"/>
                            <xctk:DoubleUpDown Grid.Column="1" 
                                             Value="{Binding DefaultClosedLineWidth}" 
                                             Minimum="1.0" 
                                             Maximum="100.0"
                                             Increment="1.0"
                                             FormatString="F1"/>
                        </Grid>
                    </StackPanel>
                </Expander>

                <!-- Advanced Settings -->
                <Expander Header="Advanced Settings" IsExpanded="False" Margin="10">
                    <StackPanel Margin="10">
                        <!-- Default Location Jump Downsample -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Jump Zoom Downsample:"/>
                            <xctk:DoubleUpDown Grid.Column="1" 
                                             Value="{Binding DefaultLocationJumpDownsample}" 
                                             Minimum="1.0" 
                                             Maximum="64.0"
                                             Increment="1.0"
                                             FormatString="F1"/>
                        </Grid>

                        <!-- Adjacent Location Radius Scalar -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Adjacent Section Radius:"/>
                            <xctk:DoubleUpDown Grid.Column="1" 
                                             Value="{Binding AdjacentLocationRadiusScalar}" 
                                             Minimum="0.1" 
                                             Maximum="2.0"
                                             Increment="0.1"
                                             FormatString="F2"/>
                        </Grid>

                        <!-- Num Curve Interpolation Points -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Curve Display Points:"/>
                            <xctk:IntegerUpDown Grid.Column="1" 
                                              Value="{Binding NumClosedCurveInterpolationPointsForDisplay}" 
                                              Minimum="2" 
                                              Maximum="20"/>
                        </Grid>

                        <!-- Pen Simplify Threshold -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Pen Simplify Threshold:"/>
                            <xctk:IntegerUpDown Grid.Column="1" 
                                              Value="{Binding PenSimplifyThreshold}" 
                                              Minimum="1" 
                                              Maximum="100"/>
                        </Grid>

                        <!-- Min Radius -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Minimum Radius:"/>
                            <xctk:DoubleUpDown Grid.Column="1" 
                                             Value="{Binding MinRadius}" 
                                             Minimum="0.1" 
                                             Maximum="10.0"
                                             Increment="0.1"
                                             FormatString="F2"/>
                        </Grid>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Reset to Defaults" 
                    Width="120" 
                    Height="30" 
                    Margin="5"
                    Command="{Binding ResetToDefaultsCommand}"/>
            <Button Content="Apply" 
                    Width="80" 
                    Height="30" 
                    Margin="5"
                    Click="ApplyButton_Click"/>
            <Button Content="OK" 
                    Width="80" 
                    Height="30" 
                    Margin="5"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    Margin="5"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

