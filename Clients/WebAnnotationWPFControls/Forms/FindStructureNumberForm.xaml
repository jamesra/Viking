<Window x:Class="WebAnnotation.UI.Forms.FindStructureNumberForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:local="clr-namespace:WebAnnotation.UI.Forms"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="350"
             SizeToContent="WidthAndHeight"
             MinWidth="350" MinHeight="120"
             FontSize="14"
             Topmost="True"
             Title="Find Structure"
             FocusManager.FocusedElement="{Binding ElementName=StructureTextBox}">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Structure #:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Margin="0" Name="StructureTextBox" Width="120" 
                         PreviewTextInput="StructureTextBox_PreviewTextInput"
                         TextChanged="StructureTextBox_OnTextChanged"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button x:Name="GoButton" Content="Go!" Margin="5" Width="80" Click="Go_Button_Click" IsDefault="True" IsEnabled="False"/>
                <Button Content="Close" Margin="5" Width="80" Click="Close_Button_Click" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
