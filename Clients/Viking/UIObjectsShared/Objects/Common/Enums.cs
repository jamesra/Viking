namespace Viking.Common
{
    /// <summary>
    /// Categories for extension tabs. Determines which tab controls display the extension by default
    /// </summary>
    public enum TABCATEGORY
    {
        NAVIGATION = 0,
        ACTION = 1,
        CUSTOM = 2
    };
}
